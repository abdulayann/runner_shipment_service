## This file will be used to deploy the service repo

# General application info
organization: dpwhotfsonline
productName: runner
azureProject: DTLP
appName: runner-shipment-service
repoName: runner_shipment_service
repoId: cdeb2aab-0be8-4566-bbba-421a13f55ec5
serviceType: backend
language: JAVA
buildTool: maven
branching: Release
infra: K8s
containerRegistryURL: acrcargoesrunner.azurecr.io
libraryVariableGroupName: runner_runner_shipment_service
libraryVariableGroupId: 342
allEnvironments: ['dev', 'qa', 'staging', 'prod','dr']
bluegreen: True
artifactAliasName: _config
artifactVstsFeedPublish: 23a36ede-eaaf-42ed-b849-690d3430f272/fbd15528-c4b5-49c1-beea-5b436be8317c
containerRegistrySC: ACR_Cargoes Runner
artifactsFeeds: runner-artifactory
jdkVersion: 1.17
mavenOptions: -Xmx3072m $(MAVEN_OPTS)
CONFIG_FOLDER_PATH: src/main/resources/

# Service configuration
service:
  name: runner-shipment-service-svc
  type: ClusterIP
  port: 80
  targetPort: 8080

# ConfigMap configuration
configMap:
  name: runner-shipment-service-cm
  data:
    NEW_RELIC_ENABLED: true
    NEW_RELIC_LOG_ENABLED: true
    NEW_RELIC_DISTRIBUTED_TRACING_ENABLED: true
    NEW_RELIC_LOG_LEVEL: info
    SPRING_CONFIG_LOCATION: file:///app/

# Horizontal Pod Autoscaler (HPA) configuration
hpa:
  name: runner-shipment-service-hpa
  enabled: true
  avgCpuUtil: 80
  stableSec: 300
  scaleUpCount: 1
  scaleUpSec: 60

# Deployment configuration
deployment:
  name: runner-shipment-service-deployment
  imageName: runner-shipment-service
  imageTag: latest
  port: 8080
  healthCheck: /shipment-service/actuator/health
  initialDelaySeconds: 60
  
# Ingress configuration
ingress:
  name: runner-shipment-service-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: 'false'
    nginx.ingress.kubernetes.io/proxy-body-size: '40m'
    nginx.org/client-max-body-size: '40m'
    nginx.ingress.kubernetes.io/use-regex: 'true'
  rules:
    - paths:
        - path: /shipment-service
          pathType: ImplementationSpecific
          serviceName: runner-shipment-service-svc
          servicePort: 80

# SecretProvider configuration
secretprovider:
  name: runner-shipment-service-secretproviderclass
  nonProdUserAssignedIdentityID: 0abd98ec-d7a9-4390-8820-792bad7c78f3
  nonProdKeyVaultName: cargoesrunner-nonprod-kv
  prodUserAssignedIdentityID: d50883f6-2c0a-4fdb-b659-6176f5fb0be5
  prodKeyVaultName: kv-cargorunner-prod
  drUserAssignedIdentityID: d50883f6-2c0a-4fdb-b659-6176f5fb0be5
  drKeyVaultName: kv-cargorunner-prod
  tenantId: 2bd16c9b-7e21-4274-9c06-7919f7647bbb

dev:
  cicd: Full
  envName: dev
  namespace: runner-shipment-service-dev-ns
  hpa:
    minReplicas: 1
    maxReplicas: 3
  hostUrlList: 
  - dummy-dev-runner-shipment-service.dpworld.com
  configMap:
    data:
      APP_ENV: dev
      NEW_RELIC_APP_NAME: runner-shipment-service-dev
  secretName: runner-shipment-service-dev-SECRET
  deployment:
    replicaCount: 1
    args: tar -xvzf /tmp/app-config/config.tar -C . && cat /mnt/secrets-store/runner-shipment-service-dev-SECRET >> ~/.bashrc && . ~/.bashrc  && java -Xms256m -Xmx750m -javaagent:/usr/local/newrelic/newrelic.jar -jar runner-shipment-service.jar
    minCpu: 300m
    maxCpu: 500m
    minMem: 512Mi
    maxMem: 1Gi

qa:
  cicd: Full
  envName: qa
  namespace: runner-shipment-service-qa-ns
  hpa:
    minReplicas: 1
    maxReplicas: 3
  hostUrlList: 
  - dummy-qa-runner-shipment-service.dpworld.com
  configMap:
    data:
      APP_ENV: qa
      NEW_RELIC_APP_NAME: runner-shipment-service-qa
  secretName: runner-shipment-service-qa-SECRET
  deployment:
    replicaCount: 1
    args: tar -xvzf /tmp/app-config/config.tar -C . && cat /mnt/secrets-store/runner-shipment-service-qa-SECRET >> ~/.bashrc && . ~/.bashrc  && java -Xms256m -Xmx750m -javaagent:/usr/local/newrelic/newrelic.jar -jar runner-shipment-service.jar
    minCpu: 300m
    maxCpu: 500m
    minMem: 512Mi
    maxMem: 1Gi

staging:
  cicd: Full
  envName: staging
  namespace: runner-shipment-service-staging-ns
  hpa:
    minReplicas: 1
    maxReplicas: 3
  hostUrlList: 
  -  dummy-staging-runner-shipment-service.dpworld.com
  configMap:
    data:
      APP_ENV: staging
      NEW_RELIC_APP_NAME: runner-shipment-service-staging
  secretName: runner-shipment-service-staging-SECRET
  deployment:
    replicaCount: 1
    args: tar -xvzf /tmp/app-config/config.tar -C . && cat /mnt/secrets-store/runner-shipment-service-staging-SECRET >> ~/.bashrc && . ~/.bashrc  && java -Xms256m -Xmx750m -javaagent:/usr/local/newrelic/newrelic.jar -jar runner-shipment-service.jar
    minCpu: 300m
    maxCpu: 500m
    minMem: 512Mi
    maxMem: 1Gi

prod:
  cicd: Full
  envName: prod
  namespace: runner-shipment-service-prod-ns
  hpa:
    minReplicas: 2
    maxReplicas: 5
  hostUrlList: 
  - dummy-runner-shipment-service.dpworld.com
  configMap:
    data:
      APP_ENV: prod
      NEW_RELIC_APP_NAME: runner-shipment-service-prod
  secretName: runner-shipment-service-prod-SECRET
  deployment:
    replicaCount: 1
    args: tar -xvzf /tmp/app-config/config.tar -C . && cat /mnt/secrets-store/runner-shipment-service-prod-SECRET >> ~/.bashrc && . ~/.bashrc  && sleep 10000s
    minCpu: 300m
    maxCpu: 500m
    minMem: 512Mi
    maxMem: 1Gi

dr:
  cicd: Full
  envName: dr
  namespace: runner-shipment-service-dr-ns
  hpa:
    minReplicas: 1
    maxReplicas: 5
  hostUrlList: 
  - dummy-runner-shipment-service.dpworld.com
  configMap:
    data:
      APP_ENV: dr
      NEW_RELIC_APP_NAME: runner-shipment-service-dr
  secretName: runner-shipment-service-dr-SECRET
  deployment:
    replicaCount: 0
    args: tar -xvzf /tmp/app-config/config.tar -C . && cat /mnt/secrets-store/runner-shipment-service-dr-SECRET >> ~/.bashrc && . ~/.bashrc  && java -Xms256m -Xmx750m -javaagent:/usr/local/newrelic/newrelic.jar -jar runner-shipment-service.jar
    minCpu: 300m
    maxCpu: 500m
    minMem: 512Mi
    maxMem: 1Gi